# import sys
#
# read = sys.stdin.readline
#
# n, m = map(int, read().split())
# graph = [list(map(lambda x: {'A': 0, 'T': 1, 'G': 2, 'C': 3}[x], list(read().rstrip()))) for _ in range(n)]
# for row in graph:
#     print(*row)
